# 贡献指南

感谢您对 OpenMDtx 的关注！我们欢迎任何形式的贡献。

## 🤝 如何贡献

### 报告问题

如果您发现了 bug 或有功能建议，请：

1. 先检查 [Issues](https://github.com/Shaw529/openmdx/issues) 是否已存在相同问题
2. 如果没有，请创建新的 Issue，并使用相应的模板
3. 提供尽可能详细的信息，包括：
   - 复现步骤
   - 预期行为
   - 实际行为
   - 系统环境（操作系统、版本等）
   - 截图或错误日志

### 提交代码

#### 开发环境设置

```bash
# 1. Fork 本仓库
# 点击 GitHub 页面右上角的 Fork 按钮

# 2. 克隆您的 Fork
git clone https://github.com/YOUR_USERNAME/openmdx.git
cd openmdx

# 3. 添加上游仓库
git remote add upstream https://github.com/Shaw529/openmdx.git

# 4. 安装依赖
npm install

# 5. 启动开发服务器
npm run dev
```

#### 分支管理

```bash
# 1. 创建功能分支
git checkout -b feature/your-feature-name
# 或修复分支
git checkout -b fix/your-bug-fix

# 2. 进行开发和测试
npm run dev
npm run lint

# 3. 提交更改
git add .
git commit -m "feat: add your feature description"
# 或
git commit -m "fix: describe the bug fix"

# 4. 推送到您的 Fork
git push origin feature/your-feature-name

# 5. 在 GitHub 上创建 Pull Request
```

#### Commit 规范

我们使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 新功能
- `fix:` Bug 修复
- `docs:` 文档更新
- `style:` 代码格式调整（不影响功能）
- `refactor:` 代码重构
- `perf:` 性能优化
- `test:` 测试相关
- `chore:` 构建/工具相关

示例：
```
feat: add image drag and drop support
fix: resolve memory leak in editor component
docs: update installation guide
```

#### 代码规范

- **TypeScript**: 所有代码必须使用 TypeScript
- **ESLint**: 运行 `npm run lint` 确保代码通过检查
- **代码风格**: 遵循现有代码风格
- **注释**: 为复杂逻辑添加注释
- **测试**: 确保新功能经过充分测试

#### Pull Request 指南

1. **标题**: 使用清晰的标题，格式如 `feat: add xxx` 或 `fix: resolve xxx`
2. **描述**: 详细说明您的更改内容
3. **关联 Issue**: 如果修复了某个 Issue，在描述中提及 `Fixes #xxx`
4. **截图**: 如果涉及 UI 变更，提供前后对比截图
5. **保持简洁**: 一个 PR 只做一件事，避免大规模更改

### 代码审查

- 所有 PR 都需要经过代码审查
- 请及时响应审查意见
- 根据反馈进行修改
- 保持友好和专业的交流态度

## 📂 项目结构

```
openmdx/
├── src/
│   ├── components/          # React 组件
│   ├── contexts/            # React Context
│   ├── hooks/               # 自定义 Hooks
│   ├── extensions/          # TipTap 扩展
│   ├── locales/             # 国际化文件
│   ├── main/                # Electron 主进程
│   ├── config/              # 配置文件
│   ├── types/               # TypeScript 类型
│   └── utils/               # 工具函数
├── public/                  # 静态资源
└── ...                      # 其他配置文件
```

## 🌍 国际化

如需添加新语言：

1. 在 `src/locales/` 下创建新的语言文件（如 `fr-FR.ts`）
2. 参考 `zh-CN.ts` 或 `en-US.ts` 的格式
3. 在 `src/locales/index.ts` 中注册新语言
4. 提交 PR

## 🐛 常见开发问题

### Electron 窗口无法打开
- 检查端口是否被占用
- 清除 `dist` 和 `dist-electron` 目录后重新构建

### TipTap 扩展问题
- 查看 [TipTap 官方文档](https://tiptap.dev/docs)
- 检查扩展版本兼容性

### 构建失败
- 删除 `node_modules` 和 `package-lock.json`
- 重新运行 `npm install`

## 📜 行为准则

- 尊重所有贡献者
- 保持友好和包容的态度
- 接受建设性批评
- 关注对社区最有利的事情

## 📝 许可证

通过贡献代码，您同意您的贡献将根据项目的 [MIT License](LICENSE) 进行许可。

## 🙏 致谢

感谢所有为 OpenMDtx 做出贡献的开发者！

---

**有任何问题？** 请在 [Issues](https://github.com/Shaw529/openmdx/issues) 中提问。
